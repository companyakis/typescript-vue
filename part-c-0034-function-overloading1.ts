function sum(x: number, y: number) : number 

function sum(x: number, y: number, z: number) : number 

function sum(x: number, y: number, z: number) : number {
    
    return x + y + z
}

let result = sum(4, 12, 456)

console.log(result) // 472


export {}
